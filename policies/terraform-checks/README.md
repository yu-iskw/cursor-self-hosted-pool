# Terraform Checks

Module-native `check` blocks and `lifecycle.precondition`s live inside each
module. Optional organization OPA/Conftest policies can be added here later;
they are intentionally not required for phase one.
