# Contract expectations for cursor-worker-pool validations.
# Run from modules/cursor-worker-pool with: terraform test
# (Requires provider credentials only if resources are planned; these tests use command = plan with expect_failures where supported.)
