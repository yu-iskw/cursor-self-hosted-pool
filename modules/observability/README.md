# Observability

Optional Monitoring alert policies for Cursor worker pools. Accepts existing
notification channels; does not own organization-wide channels.

Metric filters may need adjustment per Cloud Run Worker Pool metric availability
in your project — validate in Phase 0 / staging.
